<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pacificris.usersapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UsersApp">
        <activity android:name=".Update_user"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.UsersApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SingUp"
            android:theme="@style/Theme.UsersApp.NoActionBar"></activity>
        <activity
            android:name=".UserList"
            android:label="@string/NameActionbarList"></activity>
    </application>

</manifest>